# TelegramControl — beta (không OTA công khai)

Bản beta chỉ dành thử nghiệm. Đặt `activeChannel` = `beta` trong `ota/channel.json` khi build — Magisk **không** hiện nút cập nhật cho user cài bản stable.
