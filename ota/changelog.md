# TelegramControl — changelog (stable)

## 4.21 Final

- Cập nhật qua Magisk (không cần gỡ module; giữ `config.sh`).
- Sửa reboot lặp ở boot logo (`service.sh` chạy nền).
- Chặn `/restart` cũ trong hàng đợi Telegram; chỉ nhận lệnh từ Chat ID đã cấu hình.
